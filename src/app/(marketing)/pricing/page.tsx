import { Card } from "@/components/ui";

export default function PricingPage() {
  return (
    <Card>
      <h1 className="text-xl font-semibold tracking-tight">Pricing</h1>
      <p className="mt-2 text-sm text-zinc-600">Pricing page shell for public route structure. Product plans and billing rules will be added with business logic implementation.</p>
    </Card>
  );
}
